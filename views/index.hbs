<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>
<div class="flex">
  {{#each posts}}
  <div class="post">
    <img src="{{this.picPath}}" alt="{{this.picName}}">
    <p>Título: {{this.picName}}</p>
    <p>Comentario: {{this.content}}</p>
    <p>User: {{this.creatorId.username}}</p>
  </div>
  {{/each}}
</div>
